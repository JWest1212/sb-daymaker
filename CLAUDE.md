@AGENTS.md

# SB Daymaker — canonical context (read this first)

The **authoritative project contract** for this repo is:

> **`Core Project Files/CLAUDE.md`** — v9 canon. Read it in full before doing anything.
> When it conflicts with your training instincts, it wins.

Also keep open while building:

- `Core Project Files/08_SBDaymaker_Build_Plan.md` — the phased runbook (build in order).
- `Core Project Files/sbdaymaker_tokens.css` — the design-system source of truth (also
  imported app-wide via `app/globals.css`). Never hardcode a color/font/spacing.
- `Core Project Files/sbdaymaker_schema.sql` — the data contract (run as-is; don't redesign).

(The `@AGENTS.md` import above carries this Next.js version's own rules — heed both.)
